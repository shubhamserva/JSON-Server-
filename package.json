{
  "name": "api",
  "version": "1.0.0",
  "description": "BAckend JSON server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"json-server -p 3001 -w db.json"
  },
  "author": "Shubham",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
